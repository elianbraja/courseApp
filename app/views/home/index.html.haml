.jumbotron
  .display-4
    What do you want to learn today?
    .p-4

    - if current_user
      .form-inline.my-2.my-lg-0
        = search_form_for @course_search, as: :course_search, url: courses_path do |f|
          .input-group
            = f.search_field :title_cont, class: 'form-control-sm', style: 'float: left, width: 150px;', placeholder: "Find a Course"
            %span.input-group-append
              %button.btn.btn-primary.btn-sm{:type => "submit"}
                %span.fa.fa-search{"aria-hidden" => "true"}

.card-group
  .card.card-body
    %h1
      .fa.fa-graduation-cap
    = Course.count
    online courses
    .row
    Explore a variety of fresh topics
  .card.card-body
    %h1
      .fa.fa-chalkboard-teacher
    = Course.distinct.count(:user_id)
    expert instructors
    .row
    Find the right instructor fpr you
  .card.card-body
    %h1
      .fa.fa-user-graduate
    = User.count
    active learners
    .row
    Join the knowledge sharing community
  .card.card-body
    %h1
      .fa.fa-infinity
    Lifetime access
    .row
    Learn on your schedule

%hr
%h2
  Popular courses
.card-columns
  - @courses.each do |course|
    = render 'courses/course', course: course
%hr
%h2
  Top rated courses
.card-columns
  - @recent_courses.each do |course|
    = render 'courses/course', course: course
%hr
%h2
  Most recent
.card-columns
  - @latest_courses.each do |course|
    = render 'courses/course', course: course
%hr
